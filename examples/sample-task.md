# SAMPLE-001 Add project listing endpoint

Expose a REST endpoint that lists projects for the current tenant.

Respect tenant isolation, keep the HTTP adapter thin, and add unit tests for the use case plus integration coverage for persistence behavior.
